<div id="<%= dom_id(game_key) %>">
  <%= render 'shared/paper' do %>
    <div class="p-3">
      <div class="grid grid-cols-2 gap-2 h-24">
        <div class="w-full bg-gray-400 rounded-lg"></div>
        <div class="flex flex-col items-start ml-4">
          <h4 class="text-xl font-semibold"><%= game_key.name %></h4>
          <% if key_visible %>
            <p class="text-sm"><%= game_key.key %></p>
          <% else %>
            <%= form_with url: claim_game_key_path(game_key), method: :patch, data: { turbo_confirm: t(".confirm-claim") } do %>
              <%= hidden_field_tag :view, 'card' %>
              <%= render 'shared/button', type: 'submit' do %>
                <%= t(".claim") %>
              <% end %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>
