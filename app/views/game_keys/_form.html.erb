<%= render 'shared/paper' do %>
  <div class="p-3">
    <%= form_with(model: game_key, builder: CustomFormBuilder) do |form| %>
      <% if game_key.errors.any? %>
        <div id="error_explanation" class="mb-4 p-4 bg-red-50 dark:bg-red-900/30 border border-red-200 dark:border-red-800 rounded-md">
          <h2 class="text-red-800 dark:text-red-200 font-medium"><%= pluralize(game_key.errors.count, "error") %> prohibited this game_key from being saved:</h2>

          <ul class="mt-2 text-red-600 dark:text-red-300 text-sm">
            <% game_key.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.container_for :name do %>
        <%= form.label :name %>
        <%= form.text_field :name %>
        <%= form.error_for :name %>
      <% end %>

      <%= form.container_for :key do %>
        <%= form.label :key %>
        <%= form.text_field :key %>
        <%= form.error_for :key %>
      <% end %>

      <%= form.container_for :server_ids do %>
        <%= form.label :server_ids, t(".servers") %>
        <%= form.select :server_ids, options_from_collection_for_select(current_user.servers, :id, :name), {}, { multiple: true } %>
        <%= form.error_for :server_ids %>
      <% end %>

      <div class="actions">
        <%= render 'shared/button', type: 'submit' do %>
          <%= t(".submit") %>
        <% end %>
      </div>
    <% end %>
  </div>
<% end %>
