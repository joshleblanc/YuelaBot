<div class="p-3">
  <%= form_with model: current_user, url: current_user_toggle_afk_url do |form| %>
    <%= form.fields_for :afk do |afk_form| %>
      <%= afk_form.label :comment, "Need to stp away from the keyboard?", class: "block text-sm font-medium text-gray-700" %>
      <%= content_tag :div, class: "mt-1 flex rounded-md p-2 m-2", data: tippy_data do %>
        <%= afk_form.text_field :comment, disabled: current_user.afk?, class: "disabled:opacity-50 focus:ring-indigo-500 focus:border-indigo-500 flex-1 block w-full rounded-md sm:text-sm border-gray-300", placeholder: "I'm late for a very important date"  %>
      <% end %>
      <%= render ButtonComponent.new do %>
        <%= text %>
      <% end %>
    <% end %>
  <% end %>
</div>


<%#= component_controller class: "p-3" do %>
<!--  <label for="afk_reason" class="block text-sm font-medium text-gray-700">-->
<!--    Need to step away from the keyboard?-->
<!--  </label>-->
  <%#= content_tag :div, class: "mt-1 flex rounded-md p-2 m-2", data: tippy_data do %>
<!--    <input type="text" <%#= afk? && "disabled" %> id="afk_reason" class="disabled:opacity-50 focus:ring-indigo-500 focus:border-indigo-500 flex-1 block w-full rounded-md sm:text-sm border-gray-300" placeholder="I'm late for a very important date">-->
  <%# end %>
  <%#= render ButtonComponent.new(type: "button", data: { reflex: "click->AfkButtonComponent#toggle", key: key  }) do %>
    <%#= text %>
  <%# end %>
<%# end %>

